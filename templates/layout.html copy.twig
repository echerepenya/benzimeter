{% extends 'base.html.twig' %}

{% block title %}Заголовок{% endblock %}

{% block body %}

<div class="container-fluid">
    <div class="row flex-nowrap">
        <div class="col-auto col-md-3 col-xl-2 px-sm-2 px-0 bg-dark">
            <div class="d-flex flex-column align-items-center align-items-sm-start px-3 pt-2 text-white min-vh-100">
                <a href="/" class="d-flex align-items-center pb-3 mb-md-0 me-md-auto text-white text-decoration-none">
                    <span class="fs-5 d-none d-sm-inline">Дневник заправок</span>
                </a>
                <ul class="nav nav-pills flex-column mb-sm-auto mb-0 align-items-center align-items-sm-start" id="menu">
                    <li class="nav-item">
                        <a href="{{ path('homepage') }}" class="nav-link align-middle px-0 {% if (app.request.pathInfo == path('homepage')) %} text-white{% endif %}">
                            <i class="fs-4 bi-house"></i> <span class="ms-1 d-none d-sm-inline">Записи</span>
                        </a>
                    </li>
                    <li>
                        <a href="{{ path('car') }}" class="nav-link align-middle px-0 {% if (app.request.pathInfo == path('car')) %} text-white{% endif %}">
                            <i class="fs-4 bi-table"></i> <span class="ms-1 d-none d-sm-inline">Автомобили</span></a>
                    </li>
                    <li>
                        <a href="{{ path('petrol_station') }}" class="nav-link align-middle px-0 {% if (app.request.pathInfo == path('petrol_station')) %} text-white{% endif %}">
                            <i class="fs-4 bi-people"></i> <span class="ms-1 d-none d-sm-inline">Заправочные станции</span> </a>
                    </li>
                    {#
                    <li>
                        <a href="#submenu2" data-bs-toggle="collapse" class="nav-link px-0 align-middle ">
                            <i class="fs-4 bi-bootstrap"></i> <span class="ms-1 d-none d-sm-inline">Посты</span></a>
                        <ul class="collapse nav flex-column ms-1" id="submenu2" data-bs-parent="#menu">
                            <li class="w-100">
                                <a href="#" class="nav-link px-0"> <span class="d-none d-sm-inline">Вьезд</span> 1</a>
                            </li>
                            <li>
                                <a href="#" class="nav-link px-0"> <span class="d-none d-sm-inline">Вьезд</span> 2</a>
                            </li>
                        </ul>
                    </li>
                    #}
                </ul>
                <hr>
                <div class="dropdown pb-4">
                    <a href="#" class="d-flex align-items-center text-white text-decoration-none dropdown-toggle" id="dropdownUser1" data-bs-toggle="dropdown" aria-expanded="false">
                        {# <img src="https://github.com/mdo.png" alt="hugenerd" width="30" height="30" class="rounded-circle"> #}
                        <span class="d-none d-sm-inline mx-1">{# if is_granted('ROLE_USER') #}</span>
                    </a>
                    <ul class="dropdown-menu dropdown-menu-dark text-small shadow">
                        <li><a class="dropdown-item" href="#">Настройки</a></li>
                        <li><a class="dropdown-item" href="#">Профиль</a></li>
                        <li>
                            <hr class="dropdown-divider">
                        </li>
                        <li><a class="dropdown-item" href="{{ path('app_logout') }}">Выход</a></li>
                    </ul>
                </div>
            </div>
        </div>
        <div class="col py-3 content">
            {% block content %}{% endblock %}
        </div>
    </div>
</div>

{% endblock %}
